{
  "name": "class-validators",
  "version": "1.0.0",
  "description": "ES Decorators for function param validation",
  "main": "lib/index.js",
  "scripts": {
    "test": "(apm test) && (flow check) && (eslint . ) && (prettier --list-different src/*.js)",
    "clean": "rm -rf lib",
    "build": "sb-babel-cli src -o lib",
    "watch": "npm run build -- --watch"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/steelbrain/class-validators"
  },
  "keywords": [
    "cli",
    "command",
    "app"
  ],
  "author": "steelbrain <i@steelbrain.me>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/steelbrain/class-validators/issues"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-steelbrain": "^5.0.2",
    "eslint-config-steelbrain": "^5.0.2",
    "flow-bin": "^0.66.0",
    "sb-babel-cli": "^1.0.0"
  }
}
